ALWAYS (FORALL fid.
  (
    (Request(fid) IMPLIES EVENTUALLY[0,5] Delete(fid))
    AND
    ((Use(fid, "marketing") OR Collect(fid, "marketing")) IMPLIES ((NOT Revoke(fid, "marketing")) SINCE (Consent(fid, "marketing"))))
  )
)
